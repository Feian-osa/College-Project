[1mdiff --git a/inventory.h b/inventory.h[m
[1mindex 93fa42b..fe7a26a 100644[m
[1m--- a/inventory.h[m
[1m+++ b/inventory.h[m
[36m@@ -11,33 +11,33 @@[m
 #include <iostream>[m
 #include "Utilities.h"[m
 [m
[31m-// Avoid `using namespace std;` in headers to prevent symbol pollution in consumers[m
[32m+[m[32musing namespace std;[m
 [m
 // ===== InventoryItem Class =====[m
 // Represents a single item in the warehouse inventory[m
 [m
 class InventoryItem {[m
 private:[m
[31m-    std::string itemID;           // Unique item identifier (e.g., ITM001)[m
[31m-    std::string itemName;         // Name of the item[m
[31m-    std::string category;         // Category (e.g., Electronics, Parts)[m
[32m+[m[32m    string itemID;           // Unique item identifier (e.g., ITM001)[m
[32m+[m[32m    string itemName;         // Name of the item[m
[32m+[m[32m    string category;         // Category (e.g., Electronics, Parts)[m
     int quantity;            // Current stock quantity[m
     int reorderLevel;        // Minimum quantity before alert[m
     double unitPrice;        // Price per unit[m
[31m-    std::string entryDate;        // Date when item was added[m
[32m+[m[32m    string entryDate;        // Date when item was added[m
 [m
 public:[m
     // Constructor[m
     InventoryItem();[m
     [m
     // ===== Getters =====[m
[31m-    std::string getItemID() const { return itemID; }[m
[31m-    std::string getItemName() const { return itemName; }[m
[31m-    std::string getCategory() const { return category; }[m
[32m+[m[32m    string getItemID() const { return itemID; }[m
[32m+[m[32m    string getItemName() const { return itemName; }[m
[32m+[m[32m    string getCategory() const { return category; }[m
     int getQuantity() const { return quantity; }[m
     int getReorderLevel() const { return reorderLevel; }[m
     double getUnitPrice() const { return unitPrice; }[m
[31m-    std::string getEntryDate() const { return entryDate; }[m
[32m+[m[32m    string getEntryDate() const { return entryDate; }[m
     [m
     // Calculate total value of this item (quantity * unitPrice)[m
     double getTotalValue() const { return quantity * unitPrice; }[m
[36m@@ -46,22 +46,22 @@[m [mpublic:[m
     bool isLowStock() const { return quantity <= reorderLevel; }[m
     [m
     // ===== Setters =====[m
[31m-    void setItemID(const std::string& id) { itemID = id; }[m
[31m-    void setItemName(const std::string& name) { itemName = name; }[m
[31m-    void setCategory(const std::string& cat) { category = cat; }[m
[32m+[m[32m    void setItemID(const string& id) { itemID = id; }[m
[32m+[m[32m    void setItemName(const string& name) { itemName = name; }[m
[32m+[m[32m    void setCategory(const string& cat) { category = cat; }[m
     void setQuantity(int qty) { quantity = qty; }[m
     void setReorderLevel(int level) { reorderLevel = level; }[m
     void setUnitPrice(double price) { unitPrice = price; }[m
[31m-    void setEntryDate(const std::string& date) { entryDate = date; }[m
[32m+[m[32m    void setEntryDate(const string& date) { entryDate = date; }[m
     [m
     // Display item in table format[m
     void display() const;[m
     [m
     // Convert item to file format (for saving)[m
[31m-    std::string toFileString() const;[m
[32m+[m[32m    string toFileString() const;[m
     [m
     // Parse item from file format (for loading)[m
[31m-    void fromFileString(const std::string& data);[m
[32m+[m[32m    void fromFileString(const string& data);[m
 };[m
 [m
 [m
[36m@@ -70,17 +70,17 @@[m [mpublic:[m
 [m
 class InventoryManager {[m
 private:[m
[31m-    std::vector<InventoryItem> items;        // Container for all items[m
[31m-    std::string inventoryFile;               // Filename for persistent storage[m
[32m+[m[32m    vector<InventoryItem> items;        // Container for all items[m
[32m+[m[32m    string inventoryFile;               // Filename for persistent storage[m
     [m
     // Private helper functions[m
     void loadInventory();               // Load items from file[m
     void saveInventory();               // Save items to file[m
[31m-    InventoryItem* findItemByID(const std::string& id);  // Search by ID[m
[32m+[m[32m    InventoryItem* findItemByID(const string& id);  // Search by ID[m
     [m
 public:[m
     // Constructor and Destructor[m
[31m-    InventoryManager(const std::string& filename = "inventory.txt");[m
[32m+[m[32m    InventoryManager(const string& filename = "inventory.txt");[m
     ~InventoryManager();[m
     [m
     // ===== CRUD Operations =====[m
[36m@@ -106,7 +106,7 @@[m [mpublic:[m
     // ===== Getters for other modules =====[m
     [m
     // Get all items (used by Report and Alert modules)[m
[31m-    const std::vector<InventoryItem>& getAllItems() const { return items; }[m
[32m+[m[32m    const vector<InventoryItem>& getAllItems() const { return items; }[m
     [m
     // Get total number of items[m
     int getTotalItemsCount() const { return items.size(); }[m
[36m@@ -115,7 +115,7 @@[m [mpublic:[m
     double getTotalInventoryValue() const;[m
     [m
     // Get a specific item by ID (for reports)[m
[31m-    const InventoryItem* getItemByID(const std::string& id) const;[m
[32m+[m[32m    const InventoryItem* getItemByID(const string& id) const;[m
 };[m
 [m
 #endif // INVENTORY_H[m
\ No newline at end of file[m
